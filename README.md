# ProjectX
## Участники. Группа 0907-35

Ахметзянов Ислам
Бариев Илдус
Багаветдинов Арслан

## Компиляция
Для компиляции используйте команду:
```
gcc main.c filesystem.c -o fs_program
```
## filesystem.h
Заголовочный файл. Содержит объявлление функций:
-create_open_file
-view_file
-delete_file
-add_file
-modify_file

## filesystem.c
### Функция create_open_file
Функция принимает имя файла. Если файл не существует, он создается. Если файл существует, он открывается.  Функция возвращает открытый файловый дескриптор.

### Функция view_file
Функция принимает имя файла и открывает его для чтения. Чтение начинается с начала файла.  После каждой строки добавляется символ новой строки.

### Функция delete_file
Функция принимает имя файла. Если файл существует, он удаляется.  В противном случае возвращается сообщение об ошибке.

### Функция add_file
Функция принимает имя нового файла. Если файл с таким именем уже существует, возвращается сообщение об ошибке.  Затем функция принимает имя файла, чье содержимое будет записано в новый файл.
ПРИМЕР:

```
имя нового файла
имя существующего файла
```

### Функция modify_file
Функция принимает имя файла. Функция проверяет, существует ли файл. Если файл существует, его содержимое перезаписывается. В противном случае возвращается сообщение об ошибке.

## Интерфейс
Интерфейс реализован в консоли. Чтобы выбрать ту или иную функцию, требуется написать соответствуещую цифру
![изображение](https://github.com/user-attachments/assets/c09fd7d8-2951-4eb3-a27b-705e45d86233)


## Подключение библиотеки
Чтобы подклюить функции нужно в начале программы написа
```
#include "filesystem.h
```

### PS: Пожалуйста, поставьте 5! (Ислам, Илдус, Арслан)
